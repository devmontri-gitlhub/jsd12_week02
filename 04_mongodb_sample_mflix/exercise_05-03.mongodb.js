// เลือกฐานข้อมูล sample_mflix
use("sample_mflix");

// ค้นหาภาพยนตร์ที่ออกฉายในช่วงปี 1955 - 1965
// เรียงลำดับปีจากน้อยไปมาก
// และแสดงผลลัพธ์เพียง 3 รายการ
db.movies.find({
    year: { $gt: 1954, $lt: 1966 }
})
.sort({ year: 1 })
.limit(3);